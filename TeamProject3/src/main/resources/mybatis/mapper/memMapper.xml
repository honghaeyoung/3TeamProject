<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="springboot.project.dao.MemberDao">
   
   <select id="login" parameterType="map" resultType="springboot.project.dto.MemberDto">
     select * from member where memberid=#{memberid} and memberpw=#{memberpw}
    </select>
   
   <update id="updateMem" parameterType="springboot.project.dto.MemberDto">
   update member set memberpw=#{memberpw}, name=#{name}, birth=#{birthday},address=#{address}
   where id=#{memberid}
   </update>
   
   
 
 
 
</mapper>